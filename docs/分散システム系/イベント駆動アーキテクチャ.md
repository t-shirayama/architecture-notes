# イベント駆動アーキテクチャ

## 概要

状態変化や出来事をイベントとして発行し、購読側が非同期に反応する構成です。

## 解決したい課題

- 処理やデータをどの単位に分け、どのように通信させるかが主要な論点です。
- 変更影響、運用負荷、理解しやすさのバランスを取る
- 適用範囲と責務境界を明確にする

## 基本構成

| 要素 | 責務 |
| --- | --- |
| Event Producer | 出来事をイベントとして発行する側 |
| Event Broker | イベントの配送、保持、ルーティングを担う基盤 |
| Event Consumer | イベントを購読して処理する側 |
| Event Schema | イベント名、意味、データ形式の契約 |

## Mermaid図

```mermaid
flowchart LR
    A[利用者・呼び出し元] --> B[イベント駆動アーキテクチャ]
    B --> C[主要コンポーネント]
    C --> D[データ・外部システム]
```

この図は全体像を簡略化したものです。実際には、非機能要件、組織体制、利用技術によって境界や責務が変わります。

## 向いている場面

- スケール、可用性、チーム独立性、運用負荷のバランスを判断する場面に向きます。
- 変更や障害の影響範囲を意識して設計したい
- チーム内で構成要素の責務を共通認識にしたい

## 向いていない場面

- 課題が小さく、導入コストのほうが大きい
- 境界や責務を運用で守る体制がない
- 名前だけ導入して実装方針やレビュー観点が変わらない

## メリット

- 責務の分離により変更箇所を見つけやすい
- 設計判断の観点をチームで共有しやすい
- 適用条件が合えば、保守性や拡張性を高めやすい

## デメリット

- 抽象化や構成要素が増え、初期コストがかかる
- 境界設計を誤ると、かえって複雑になる
- 小さなシステムでは過剰設計になりやすい

## 類似アーキテクチャとの違い

| 比較対象 | 違い |
| --- | --- |
| Publish-Subscribe | Publish-Subscribeは関連する問題領域で使われる。イベント駆動アーキテクチャは「状態変化や出来事をイベントとして発行し、購読側が非同期に反応する構成です。」点を主に扱うため、導入目的と責務境界を分けて判断する |
| Event Sourcing | Event Sourcingは関連する問題領域で使われる。イベント駆動アーキテクチャは「状態変化や出来事をイベントとして発行し、購読側が非同期に反応する構成です。」点を主に扱うため、導入目的と責務境界を分けて判断する |
| Message Queue Architecture | Message Queue Architectureは関連する問題領域で使われる。イベント駆動アーキテクチャは「状態変化や出来事をイベントとして発行し、購読側が非同期に反応する構成です。」点を主に扱うため、導入目的と責務境界を分けて判断する |

## 実務での判断ポイント

- 何を守りたいのか、何を変えやすくしたいのかを先に決める
- 導入後に責務境界をレビューできるルールを用意する
- 既存システムへは小さな範囲から適用し、効果を確認する

## 参考

- Martin Fowler, [What do you mean by Event-Driven?](https://martinfowler.com/articles/201701-event-driven.html)
